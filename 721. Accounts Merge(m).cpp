#include <iostream>
#include <vector>
#include <string>
#include <unordered_map>

using namespace std;

/*
 * make a graph of mail
 * use DFS
 */
vector<vector<string>> accountsMerge(vector<vector<string>>& accounts) {
    string name;

    for(auto account:accounts){
        name = account[0];
        for(int i=1; i<account.size();i++){


        }
    }
}